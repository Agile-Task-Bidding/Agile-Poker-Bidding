import React from 'react';
import { Typography } from '@material-ui/core';

const ClosedSubpage = (props) => {
    return <Typography>The room is inactive</Typography>
};

export default ClosedSubpage;